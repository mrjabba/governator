<h1 class="resource">New resource</h1>

<%= form_for(@resource) do |f| %>
  <%= render 'shared/error_messages_resource', :object => f.object %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <%= f.hidden_field :app_id %>

<div class="actions">
    <%= f.submit "Create", :class => "button round" %>
    <input type="button" id="cancelresourceNew" value="Cancel" class="button round" />
  </div>
<% end %>

