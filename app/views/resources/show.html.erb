<h1 class="app"><%= link_to "#{@resource.app.name}", app_path(@resource.app.id) %></h1>
<h1 class="resource indent1"><%= @resource.name %></h1>

<div class="actions">
  <nav class="round">
    <ul>
        <li><%= link_to "Edit", "#{@resource.id}/edit", :class => "action_button round"  %></li>
    </ul>
  </nav>
</div>


  <div class="field">
    <label>Name </label>
    <span><%= @resource.name %> </span>
  </div>





