<h1 class="app">New Application</h1>

<%= form_for [@appgroup, @app] do |f| %>
  <%= render 'shared/error_messages_app', :object => f.object %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>


<div class="actions">
    <%= f.submit "Create", :class => "button round" %>
    <input type="button" id="cancelappNew" value="Cancel" class="button round" />
  </div>
<% end %>

